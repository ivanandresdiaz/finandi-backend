// src/auth/roles.enum.ts

export enum Roles {
  POTENCIAL = 'potencial',
  CLIENTE = 'cliente',
  CLIENTE_DESTACADO = 'cliente_destacado',
  EJECUTIVO = 'ejecutivo',
  LIDER = 'lider',
  ADMIN = 'admin',
}
